spring:
#  spring-boot 는 별도의 설정이 없을 시, default 로 memory 모드로 실행함.
#  datasource:
#    url: jdbc:h2:mem:jpashop # Test 시에는 memory-DB 를 사용
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create                   # application 실행 시 모든 엔티티를 지우고 다시 생성
#    properties:
#      hibernate:
#        #        show_sql: true                  # sout 으로 로그 찍기
#        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug            # log
    org.hibernate.type: trace           # query log 찍을 때 data parameter type 확인
